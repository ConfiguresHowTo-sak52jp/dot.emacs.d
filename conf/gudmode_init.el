;;----------------------------------------------
;; gud-modeの設定
;;----------------------------------------------
;;-------- GDBの設定 ----------------
(when (display-graphic-p)
  (setq-default gdb-many-windows t) ;; GraphicsIFでの起動時にマルチウインドウモードになる
  )

